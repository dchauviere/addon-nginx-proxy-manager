text = True
non-interactive = True
authenticator = webroot
webroot-path = /data/letsencrypt-acme-challenge
logs-dir = /data/logs/letsencrypt
work-dir = /data/letsencrypt-workdir

{{ if .acme_server != "" }}server = {{ .acme_server }}{{ end }}
